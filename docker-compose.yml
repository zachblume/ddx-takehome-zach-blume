version: "3"

services:
    atlas-db:
        container_name: db
        image: postgres
        environment:
            POSTGRES_USER: "postgres"
            POSTGRES_PASSWORD: "postgres"
            POSTGRES_DB: "postgres"
        ports:
            - "5432:5432"
